import { makeStyles } from "@material-ui/core/styles";

const MessagesScrolling = makeStyles(() => ({
  root: {
    overflowY: "scroll",
    background: "white",
  },
}));

export default MessagesScrolling;
